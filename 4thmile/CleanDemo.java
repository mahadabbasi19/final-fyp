public class CleanDemo {

    // Already clean, well-factored code.
    // The engine reports: "Code is already clean — no changes needed."
    public int square(int n) {
        return n * n;
    }

    public String greet(String name) {
        return "Hello, " + name;
    }
}
